- if current_user.colors.empty?
  .alert.alert-notice
    Start using Linkadex by
    = link_to 'adding colors to your palette', colors_path
#home
  .groups
    = render :partial => 'group', :collection => @groups, :as => 'group'
